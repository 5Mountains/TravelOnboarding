import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  leftButton: {
    position: 'absolute',
    top: 50,
    padding: 10,
    left: 20,
  },
  rightButton: {
    position: 'absolute',
    padding: 10,
    top: 50,
    right: 20,
  },
});
